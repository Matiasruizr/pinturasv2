<%= form_with(model: materias_prima, local: true) do |form| %>
  <% if materias_prima.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(materias_prima.errors.count, "error") %> prohibited this materias_prima from being saved:</h2>

      <ul>
      <% materias_prima.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="field">
    <%= form.label :marca %>
    <%= form.text_field :marca %>
  </div>

  <div class="field">
    <%= form.label :cantidad %> (Kilos)
    <%= form.text_field :cantidad %>
  </div>

  <div class="field">
    <%= form.label :precio  %>  (Dolares)
    <%= form.text_field :precio %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
